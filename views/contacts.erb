<h1>View all Contacts</h1>

<ul>
<% Contact.all.each do |contact| %>
  <li><%= contact.first_name %>|   |<%= contact.last_name %>|
    |<%= contact.email%>| |<%= contact.number%>| |<%= contact.note%></li>
  <% end %>
</ul>
